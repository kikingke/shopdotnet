@model shopdotnet.Data.Entities.City

@{
    ViewData["Title"] = "Delete";
}

<a asp-action="DetailsState" asp-route-id="@Model.State.State_ID" class="btn btn-outline-default"><i class="fa-solid fa-arrow-left"></i> Atras</a> |

<div class="container">
    <hr />
    <div class="card" style="width: 60%;margin:0 auto;">
        <div class="card-body">
            <h2 class="text-center">Confirme eliminar el Municipio: <span class="text-danger">@Model.City_Name</span> ?</h2>
            <form asp-action="DeleteCity">
                <div class="form-group mt-4 d-flex justify-content-between">
                    <input type="hidden" asp-for="City_ID" />
                    <a asp-action="DetailsState" asp-route-id="@Model.State.State_ID" class="btn btn-outline-default"><i class="fa-solid fa-ban"></i> Cancelar</a>
                    <button type="submit" value="Borrar" class="btn btn-danger"><i class="fa-solid fa-delete-left"></i> Eliminar</button>
                </div>
            </form>
        </div>
    </div>
</div>
